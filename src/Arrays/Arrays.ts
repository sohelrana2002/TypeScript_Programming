const name: string[] = [];
name.push("Sohel");
name.push("Rana");

console.log(name);

// Readonly
// - The readonly keyword can prevent arrays from being changed.
const userName: readonly string[] = ["Rolex"];
// userName.push("Suria");
// The readonly keyword can prevent arrays from being changed.
console.log(userName);
